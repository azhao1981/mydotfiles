#!/bin/bash

BRANCH=feature_im_993_moniter_write
# BRANCH=feature_im_1149_real_msg_count
# BRANCH=feature_im_1173_tmp_session
# BRANCH=feature_im_1179_customer_history_msg
# BRANCH=hotfix_im_1204_ios_device_rescue
cd ~/udesk/udesk_proj
git checkout $BRANCH || (git checkout -b $BRANCH && git push -u origin $BRANCH)

cd ~/udesk/udesk_im
git checkout $BRANCH || (git checkout -b $BRANCH && git push -u origin $BRANCH)